/*
  FileArray is an array of files from a folder
  removalString is a temp string to remove from the array
*/
export const findIndex = (
  FileArray: string[],
  removalString: string
): number => {
  return 1;
};
